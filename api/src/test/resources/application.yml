spring:
  datasource:
    url: jdbc:tc:postgresql:12.8:///

  flyway:
    default-schema: public
    schemas: public
    locations:
      - classpath:test_migrations/
      - classpath:db/migration
    baseline-version: 0.0
